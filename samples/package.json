{
  "author": "Laurent Pellegrino",
  "devDependencies": {
    "bower": "^1.8.4"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "clean": "rm -rf build/",
    "prebuild": "bower update && cp -r ../bower_components ./bower_components/noticeable-widget/",
    "build": "yarn run clean && polymer build"
  },
  "version": "1.0.0"
}
