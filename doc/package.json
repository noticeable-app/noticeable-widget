{
  "author": "Laurent Pellegrino",
  "devDependencies": {
    "@polymer/iron-component-page": "^3.0.0-pre.21",
    "@webcomponents/webcomponentsjs": "^2.0.2"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "analyze": "polymer analyze --root ../ --entrypoint noticeable-widget-popup.js > analysis.json",
    "prebuild": "yarn install && yarn run analyze",
    "build": "polymer build",
    "postbuild": "cp analysis.json build/"
  },
  "version": "2.0.0"
}
